SELLERSPRITE_USERNAME=
SELLERSPRITE_PASSWORD=

# Hours to reuse the last confirmed SellerSprite login state.
# 4 is the default. Set to 0 to force login confirmation on every run.
SELLERSPRITE_LOGIN_TTL_HOURS=4

DebugMode=false
